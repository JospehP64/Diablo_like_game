using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MissionSystem : MonoBehaviour
{

    //RECUERDA: SI EL MISSIONSYSTEM RECIBE LA SOLICITUD DE LA LLAMADA DEL EVENTO (EVENTMANAGER), ACTIVA SU CODIGO
    //COMPARALO CON UN ORDENADOR CONECTANDOSE A LA RED DE UN SERVIDOR, SIENDO EL SERVIDOR EL EVENTMANAGER
    //EJEMPLO: PC01 => SOLICITUD (HAYMISION) => SERVIDOR => ESPACIODISPONIBLE => SE CONECTA A LA RED (SE ACTIVA EL TOGGLE DE LA MISION)
    //[SerializeField] EventManagerSO event;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

}
