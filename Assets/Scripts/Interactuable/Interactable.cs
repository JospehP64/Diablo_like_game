using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public interface Interactable
{
    // Start is called before the first frame update
    //"I_INTERACTABLE" (IENUMERATOR) (Interface da la cualidad de listar metodos que cumplen con todo aquello que sea de un tipo (para está ocasión. interactuable)

    public void Interactyar(Transform interactor);
    
}
